<UserControl x:Class="Installer.View.SelectApplication"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Installer.View"
             xmlns:viewModel="clr-namespace:Installer.ViewModel"
             d:DataContext="{d:DesignInstance viewModel:SelectApplicationViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="0, 8">
            <ListView ItemsSource="{Binding Applications, Mode=OneWay}">
                <ListView.Resources>
                    <DataTemplate x:Key="InstallButtonTemplate">
                        <DockPanel>
                            <Button>
                                Install
                            </Button>
                        </DockPanel>
                    </DataTemplate>
                </ListView.Resources>
                <ListView.View>
                    <GridView>
                        <GridViewColumn
                            DisplayMemberBinding="{Binding Path=Title}"
                            Header="Title" />
                        <GridViewColumn
                            DisplayMemberBinding="{Binding Path=Description}"
                            Header="Description"/>
                        <GridViewColumn
                            DisplayMemberBinding="{Binding Path=PublisherName}"
                            Header="Publisher"/>
                        <GridViewColumn Header="Install" 
                            CellTemplate="{StaticResource InstallButtonTemplate}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>
